# Host registration for the quick-invoke plugin. The client entry is discovered
# from package.json's dsh.client metadata by the Web client module builder.
- insert:
    - id: dsh-quick-invoke
      name: dsh-quick-invoke
